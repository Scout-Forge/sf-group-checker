# Scout Forge Static API v2 — TODO

Authoritative sources: PROJECT_PRP.md and EXECUTE_PRP.md. We’ll tackle items top‑down. Each task lists concrete deliverables, checks, and notes pulled from the PRP.

## Next Up
- [ ] Task 2 — API v2 JSON Schema (Ajv)

## Data Source
- Canonical input is now `Groups_Level.xlsx` (not a prebuilt JSON file).
- We will first export a normalized `all-groups.json` used by the builder.

## Global Success Criteria
- [ ] GET `/api/v2/nations.json` returns the five nations
- [ ] Unit index returns districts; district index returns groups
- [ ] Global indexes build and load under ~250 ms on GitHub Pages (cold cache)
- [ ] Redoc renders the OpenAPI spec with no errors
- [ ] `node tools/build-static-api.mjs` exits 0 and writes `dist/api/v2/`
- [ ] CI deploy publishes to GitHub Pages on push to `main`
- [ ] XLSX conversion is deterministic and idempotent (same input → same `all-groups.json`)

---

## Task 0 — XLSX → all-groups.json exporter (Priority: Highest)
Deliverables:
- [x] `tools/xlsx-to-json.mjs` that reads `Groups_Level.xlsx` and writes a normalized `all-groups.json` (default output `all-groups.json`, overridable via `OUT_JSON` env var)
- [x] Column mapping documented in `docs/data-mapping.md` (sheet/columns → nation, unit, district, group, names)
- [x] Deterministic normalization (trim, collapse whitespace, apply `toSlug` for id paths; map `british-scouting-overseas` → `overseas` for path segment only)
Normalization rules:
- [x] Trim strings; collapse internal whitespace; preserve original `name` alongside derived `slug`
- [x] Derive hierarchical structure: Nation → Unit → District → Groups[]
- [x] De-duplicate by normalized `name`/`slug` within each level
Notes:
- Preferred XLSX lib: `xlsx` (SheetJS) or `exceljs`. Add dependency before implementing.
Checks:
- [x] Running `node tools/xlsx-to-json.mjs` produces `all-groups.json`
- [x] Spot-check sample rows against output structure

## Task 1 — Slug utility + tests (Priority: High)
Deliverables:
- [x] `tools/slug.js` exporting `toSlug(name: string): string`
- [x] `tests/slug.test.mjs` covering core rules
Rules (from PRP):
- lowercase
- replace `&` with `and`
- remove non `a-z0-9`
- collapse hyphens
- trim leading/trailing hyphens
Example implementation (from PRP pseudocode):
```js
export function toSlug(s) {
  return s.toLowerCase()
    .replace(/&/g, "and")
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/-+/g, "-")
    .replace(/^-|-$/g, "");
}
```
Checks:
- [x] Unit tests pass (Node ESM; can use `node tests/slug.test.mjs` with assert)
- [x] Edge cases: multiple symbols, ampersands, surrounding spaces, empty → ""

## Task 2 — API v2 JSON Schema (Ajv)
Deliverables:
- [ ] `schema/api-v2.schema.json` (JSON Schema 2020-12)
- [ ] `$id` present and Ajv-consumable
Subschemas to include:
- [ ] NationsList
- [ ] UnitIndexEntry
- [ ] DistrictIndexEntry
- [ ] DistrictGroupsResponse
- [ ] UnitsGlobal
- [ ] DistrictsGlobal
- [ ] SearchTokens
- [ ] SearchUnits
 - [ ] SourceGroups (structure of `all-groups.json` produced from XLSX)
Checks:
- [ ] Ajv validates example payloads for each schema
- [ ] Required fields, enums, formats, and array uniqueness modeled

## Task 3 — Static API builder
Deliverables:
- [ ] `tools/build-static-api.mjs`
- [ ] Outputs under `dist/api/v2/`
Inputs/Env:
- [ ] Reads `all-groups.json` generated by Task 0 (or `process.env.ALL_JSON` override)
Core functions:
- [ ] `idFor(nation, unit?, district?)` using `toSlug`; map `british-scouting-overseas` → `overseas`
- [ ] `writeJSON(relPath, obj)` creates dirs recursively and writes pretty JSON
Generation (case-insensitive sort by `name` everywhere):
- [ ] `nations.json` with the five nations
- [ ] Per‑nation `units/index.json` (unit entries)
- [ ] Per‑unit `districts/index.json` (district entries)
- [ ] Per‑district `index.json` with `groups: []` (group entries use fragment id `#slug-of-group`)
- [ ] Global indexes: `units/index.json`, `districts/index.json`
- [ ] Search: `search/tokens.json`, `search/units.json` (tiny client-side search maps)
- [ ] `manifest.json` with `{ version, generated, sha1 }` (sha1 across all emitted files)
Fields:
- [ ] Stable `id` (path-based for nation/unit/district; fragments for groups)
- [ ] `name`, `href`, and `parent` included where specified in PRP
Checks:
- [ ] Running `node tools/build-static-api.mjs` exits 0 and creates expected tree
- [ ] Sort order stable (case-insensitive) and slugs stable

## Task 4 — Generated JSON validator
Deliverables:
- [ ] `tools/validate-api.mjs`
Validation:
- [ ] Load all generated files and validate against `schema/api-v2.schema.json` using Ajv (+ ajv-formats)
- [ ] Enforce sort order by `name` (case-insensitive) for all lists
- [ ] Enforce uniqueness (ids and slugs where relevant)
- [ ] Exit non-zero on any validation error
 - [ ] Validate the source `all-groups.json` against `SourceGroups` schema
Checks:
- [ ] Add a `npm run validate:api` to call the validator (optional)

## Task 5 — Docs: OpenAPI + Redoc
Deliverables:
- [ ] `docs/api/openapi.yaml` (OpenAPI 3.1) with `/api/v2/...` paths
- [ ] `docs/api/index.html` (Redoc) referencing `openapi.yaml`
Checks:
- [ ] Redoc renders locally (open in browser) with no errors
- [ ] Schemas align with generated payload shapes

## Task 6 — CI/CD: GitHub Pages
Deliverables:
- [ ] `.github/workflows/deploy-pages.yml`
Workflow (on push to `main` and `workflow_dispatch`):
- [ ] Checkout
- [ ] Setup Node
- [ ] `node tools/build-static-api.mjs`
- [ ] `node tools/validate-api.mjs`
- [ ] Upload artifact `dist`
- [ ] Deploy to GitHub Pages
Checks:
- [ ] Pages publishes and serves `/api/v2/...`

## Task 7 — README updates
Deliverables:
- [ ] New README section
Must cover:
- [ ] API layout & versioning
- [ ] Slugs and ID rules (group ids use `#slug-of-group`)
- [ ] Cache busting via `?v=<manifest.hash>`
- [ ] Local rebuild instructions
- [ ] Publishing via Actions

---

## Developer Notes
- Node ESM is enabled (`"type": "module"`).
- Ajv and ajv-formats already in dependencies.
- Will need an XLSX parsing dependency (e.g., `xlsx` or `exceljs`) for Task 0.
- Prefer deterministic order everywhere and small payloads per endpoint.
- Keep outputs stable to support future Worker/D1 migration.

## Quick Commands (once implemented)
- Export source JSON: `node tools/xlsx-to-json.mjs`
- Build: `node tools/build-static-api.mjs`
- Validate: `node tools/validate-api.mjs`
- Serve locally (if needed): `npx http-server -p 8080` then open docs page.
